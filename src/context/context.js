import React, {createContext} from 'react';

export const AuthContext = createContext(null);
export const CreateTourModalShowContext = createContext(null);
export const FindTourModalShowContext = createContext(null);
